{
	"schema_version": 1,
	"id": "server-table-fragment",
	"display_name": "Server table fragment",
	"description": "Creates a server-side data-table fragment wired to a server table composable.",
	"default_path": "src/pages/(app)",
	"fields": [
		{
			"token": "NAME",
			"label": "Table name",
			"description": "Used for the page filename, table name, server table composable, and test name.",
			"hint": "Use lowercase kebab-case, e.g. user.",
			"required": true,
			"validation": {
				"pattern": "^[a-z0-9-]+$",
				"message": "Use lowercase kebab-case for table name."
			}
		}
	],
	"files": [
		{
			"template": "main.vue",
			"output_path": "{{ NAME | kebab }}.vue",
			"required": true
		},
		{
			"template": "main.test.js",
			"output_path": "{{ NAME | kebab }}.test.js",
			"label": "Unit test",
			"required": true
		}
	]
}
