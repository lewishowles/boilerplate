{
	"schema_version": 1,
	"id": "component",
	"display_name": "Component",
	"description": "Create a Vue component with a unit test, and optionally a story and the integration test that mounts it.",
	"default_path": "src/components",
	"fields": [
		{
			"token": "NAME",
			"label": "Component name",
			"hint": "Use at least two words, e.g. app-header.",
			"required": true
		}
	],
	"files": [
		{
			"template": "main.vue",
			"output_path": "{{ NAME | kebab }}.vue",
			"required": true
		},
		{
			"template": "unit.test.js",
			"output_path": "{{ NAME | kebab }}.test.js",
			"label": "Unit test",
			"required": true
		},
		{
			"template": "story.vue",
			"output_path": "{{ NAME | kebab }}.story.vue",
			"label": "Story (mounted by the integration test)",
			"required": false
		},
		{
			"template": "integration.pw.js",
			"output_path": "{{ NAME | kebab }}.pw.js",
			"label": "Integration test",
			"required": false
		}
	]
}
